# S3 Bucket Management Script

This script creates an S3 bucket, uploads objects, lists them, and then deletes the bucket. It demonstrates basic S3 operations using Boto3.

## Prerequisites

- AWS credentials with permissions to create and delete S3 buckets and objects.

## Setup and Run

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
